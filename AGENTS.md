## Building this project

1. This is a Windows GPUI project, not Linux/macOS.
2. Do not build this under WSL.
3. Use the Windows Rust and MSVC toolchain with `powershell.exe` to invoke commands like `cargo` instead of WSL's toolchains.
