## Building this project

1. This is a Windows GPUI project, not Linux/macOS.
2. When building inside a WSL environment, use the Windows Rust and MSVC toolchain with powershell.exe to invoke commands like `cargo` instead of WSL's toolchains.
